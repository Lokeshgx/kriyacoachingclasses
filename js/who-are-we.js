// Who Are We Page JavaScript

(function() {
    'use strict';
    
    // Initialize who are we page on DOM ready
    function initializeWhoAreWePage() {
        loadHeader();
        loadFooter();
    }
    
    // Initialize on page load
    document.addEventListener('DOMContentLoaded', initializeWhoAreWePage);
})();

