// Testimonials Page JavaScript

(function() {
    'use strict';
    
    // Initialize testimonials page on DOM ready
    function initializeTestimonialsPage() {
        loadHeader();
        loadFooter();
    }
    
    // Initialize on page load
    document.addEventListener('DOMContentLoaded', initializeTestimonialsPage);
})();

